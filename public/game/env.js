// Supabase config for online rooms. Left empty in the repo: the deploy workflow
// injects real values from secrets; without them the game runs in local mode
// (solo vs bots + rooms across tabs of one browser).
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
